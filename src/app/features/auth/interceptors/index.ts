/**
 * Feature-scoped HTTP interceptors live here when they should only apply to
 * requests originated by this feature. Cross-cutting interceptors stay in
 * `core/interceptors` (the default place for `auth`, `tenant`, `error`, ...).
 */
export {};
